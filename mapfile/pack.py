"""
Compile a directory of map data back to a .w3m
"""

import subprocess
import os
import shutil
from mapfile import binary, config
from mapfile.unpack import CONVERT_HANDLERS
from mapfile import w3i, wts


# TODO: make this build script


if __name__ == '__main__':
    pass
